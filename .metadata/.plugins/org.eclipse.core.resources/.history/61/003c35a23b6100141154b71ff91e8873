package com.DR.dLib;

import com.badlogic.gdx.Gdx;
import com.badlogic.gdx.graphics.Color;
import com.badlogic.gdx.graphics.Texture;

public class CircleFillAnimation extends dAnimation {
	
	public CircleFillAnimation(dObject objectToAnimate, float duration, AnimationStatusListener listener, Color c)
	{
		super(objectToAnimate, duration, listener);
		this.getAnimatedObject().setColor(c);
	}

	@Override
	protected void animate(float time, float duration, float delta)
	{
		getAnimatedObject().setDimensions(dTweener.ExponentialEaseOut(time, 0, dValues.VH * 2.5f, duration)
				,dTweener.ExponentialEaseOut(time, 0, dValues.VH * 2.5f, duration));
		circleCover.setOriginCenter();
	}
	
	@Override
	public void start()
	{
		super.start();
		circleCover.setPos(Gdx.input.getX(), Gdx.input.getY());
		circleCover.setDimensions(0, 0);
	}

}
